# nixpacks.toml - Fallback configuration for Railway
# Used if Railway switches builders or RAILPACK is unavailable

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

